---
title: "多 Agent 协作冲突的教训"
date: "2026-08-03"
---

# 2026-08-03

## 多 Agent 协作冲突的教训

多 agent 同时改一份项目文件如何避免打架，这是困扰我很久的问题。虽然我也懂 worktree、branch、PR 什么的，也经常发给 AI 用，但由于长期跑三十多个终端任务，还真老是忘记完全遵循那些用法。

如果一个任务只针对一个项目还好，Codex 可以跑很久安全顺利完成。但是同一个项目多个任务分支有冲突，甚至可能 main 更新太慢，就老是出现多个 agent 任务窗口信息更新不及时老打架。

这对正规程序员来说当然是个很简单的常识，但是我却一直跑了很久歧途、搞了很多返工才知道：

- **浏览器锁**：这样不会一次性跑几十个测试把电脑干崩
- **多窗口权限分离**：分唯一主 main 管理者和多 worker 协作者，后者只允许做 branch/worktree 分任务，就相当于主管和员工的方法权限不同
- **AI 习惯分析**：多把自己日常的困扰和遇到问题发给 AI，让它结合电脑所有软件运行日志来分析你的习惯，多跑几次重复着看就记住最佳做法

> 注：这里的 AI/Agent 只指代 Codex CLI

我现在非常习惯终端里面跑任务，因为可以多个终端软件窗口同时打开，这样均分软件内存降低单一压力避免卡死崩溃，而且有任何想法都可以快速拉电脑文件和大量复制粘贴分析干活。

*—— 来自日常反思*

---

### 后续讨论补充

#### 关于主管理者 + Worker 模型的具体实践

10 个窗口，Agent 都在改同一个项目，遇到同一个文件冲突。之前我是让 10 个全部完成分支清理合并到 main，自然会有冲突。现在是第一个为主 main 管理者，只负责分支 PR 之类的合并清理检查，其他九个是组员 worker 不负责 main。

#### 不采用 branch/worktree 的替代方案

我没有用 branch/worktree，而是直接克隆出来多份代码目录，每个代码目录独立工作，配置独立的浏览器实例（基于 chrome-devtools-mcp），相当于把自己影分身为多个人。

但缺点是太占空间——几十个目录加上 node_modules 依赖估计几百 G 了。简单项目还可以。

#### 工具链探索

- **Kimi Code**：在特定场景下能自动把任务拆成不打架的 subagent
- **Orca 终端**：按 worktree 隔离
- **Beads + Gas Town**：本质就是想要唯一主 main 管理者，统一调度
- **自写软件**：专门拆解任务，多平台 CLI 并发跑，多任务下发自动分配多窗口处理